// the number of arguments a function takes

const sum = (a, b) => a + b
// the arity of sum is 2 (binary)

const inc = a => a + 1
// the arity of inc is 1 (unary)

const zero = () => 0
// the arity of zero is 0 (nulllary)